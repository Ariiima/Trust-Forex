# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env
.env.*
!.env.example
server/data.sqlite

# Local artifacts. Scratch captures land at the repo root while working a
# screen; 59 of them had accumulated there. The frames the sweep actually
# diffs against live in design/review/ref/, which this does not touch.
/*.png
tweaks.md

# review scratch renders
design/review/out/
*-v[0-9].png

# playwright-mcp scratch. Used to keep the reference screenshots here; the real
# 1:1 frames live in design/review/ref/ now, so the whole directory is scratch.
.playwright-mcp/
__pycache__/
