/* Every glyph now lives in the design-system Icon component, generated from the
 * Figma "Global icons" pack. This alias exists only so the screens that import
 * `Glyph` from here keep working. */
export { Icon as Glyph } from '../../design-system/components/Icon';
export type { IconName as GlyphName, IconName as ReferralGlyphName, IconProps as GlyphProps } from '../../design-system/components/Icon';
